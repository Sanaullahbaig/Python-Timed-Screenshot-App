# Python-Timed-Screenshot-App
Simple Python desktop app (Tkinter) to automatically capture and save full-screen screenshots every 10 minutes. Runs in a responsive background thread. Uses Pillow for image capture.
